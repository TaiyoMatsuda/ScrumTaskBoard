import { SprintfilterPipe } from './sprintfilter.pipe';

describe('SprintfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SprintfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
